<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.buyer.dao.IOthersDAO">
<select id="selectBuyerList" resultType="buyerVO">
		SELECT BUYER_ID, BUYER_NAME, BUYER_LGU
		FROM BUYER
</select>
<select id="selectLprodList" resultType="hashmap">
	 SELECT DISTINCT LPROD_GU "lprod_gu", LPROD_NM "lprod_nm"
            FROM BUYER A JOIN LPROD B
            ON A.BUYER_LGU = B.LPROD_GU
            ORDER BY LPROD_GU
</select>
</mapper>